#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jenkins-mocha/bin/jenkins.js" "$@"
  ret=$?
else 
  node  "$basedir/../jenkins-mocha/bin/jenkins.js" "$@"
  ret=$?
fi
exit $ret
